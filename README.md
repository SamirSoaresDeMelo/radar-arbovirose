# Instalamos e subimos para um repositorio com github CLI, pois é mais rápido

``` 
sudo apt install gh
```

```
gh auth login
```

```
gh repo create radar-arbovirose --public --source=. --remote=origin --push
```

